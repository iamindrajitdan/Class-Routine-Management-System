apiVersion: v1
kind: ConfigMap
metadata:
  name: crms-config
  namespace: default
data:
  db.host: "postgres"
  db.port: "5432"
  db.name: "crms_db"
  redis.host: "redis"
  redis.port: "6379"
  app.name: "Class Routine Management System"
  app.version: "1.0.0"
